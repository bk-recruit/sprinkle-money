spring:
  datasource:
    url: jdbc:mysql://kyuu.asuscomm.com:13306/sprinkle-money
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug